"""LLM subsystem for the ingestion pipeline.

Provider abstraction, concrete backends, prompt
construction, token budgeting, response parsing, and
retry logic. All public symbols are re-exported from
:mod:`unstructured_mapping.pipeline`.
"""
